import styled from 'styled-components'

import { Media } from '../../styles/global'

const Container = styled.div`
  display: flex;
  /* height: 100vh; */
`

const StyledMedia = styled(Media)`
  width: 100%;
`

export { Container, StyledMedia }
